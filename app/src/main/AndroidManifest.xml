<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".SpotifyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotifyPlaylistManager"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ─── Główna aktywność ──────────────────────────────────────── -->
        <!-- singleTask: redirect OAuth z Custom Tab wraca do TEJ instancji przez
             onNewIntent i usuwa Custom Tab z wierzchu zadania (niezawodny powrót). -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SpotifyPlaylistManager"
        android:networkSecurityConfig="@xml/network_security_config">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Callback OAuth Spotify – musi pasować do redirectUri w buildConfigField.
                 BEZ pathPattern: redirect "com.spotify.playlistmanager://callback" ma
                 pusta sciezke (callback = host), a filtr z pathPattern nie dopasowalby
                 sie do URI bez sciezki -> przegladarka nie otworzylaby aplikacji. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="${redirectHostName}"
                    android:scheme="${redirectSchemeName}" />
            </intent-filter>
        </activity>

        <!-- ─── Spotify Login Activity (wymagane przez Auth SDK) ─────── -->
        <activity
            android:name="com.spotify.sdk.android.auth.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    </application>

</manifest>
